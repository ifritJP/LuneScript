{
   let mut map:Map<int,int> = {};
   map[ 1 ] = 2;
   let mut list:List<form> = [];
   foreach val, key in map {
      list.insert( fn() {
         print( key, val );
      });
   }
   foreach func in list {
      func();
   }
}
{
   let mut map:Map<str,str> = {};
   map[ "a" ] = "b";
   let mut list:List<form> = [];
   foreach val, key in map {
      list.insert( fn() {
         print( key, val );
      });
   }
   foreach func in list {
      func();
   }
}
