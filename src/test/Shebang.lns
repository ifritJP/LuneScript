#! lnsc -shebang

import test.Sub4;

pub fn __main( argList:&List<str> ) : int {
   foreach val, index in argList {
      if index ~= 1 {
         print( val );
      }
   }
   if #argList == 1 {
      return 1;
   }
   return 0;
}
print( "hoge:" );
